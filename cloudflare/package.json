{
  "name": "sidekick-ws",
  "version": "0.0.6",
  "description": "Cloudflare Worker with Durable Object for Sidekick WebSocket",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo \"No build step required\"",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.16.0"
  }
}